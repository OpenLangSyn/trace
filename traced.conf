# traced — config
# Pruning: how long to retain trace events and how often to check
prune_hours    = 168
prune_interval = 3600

# UDP listener for network trace events (0 = disabled)
udp_port       = 9300
